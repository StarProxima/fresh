# Changelog

## 0.1.0

- Initial release.
- `FreshSession` and `SessionsSnapshot` as token-free session metadata.
- `SessionsStorage` interface with `InMemorySessionsStorage`.
- `FreshSessionController` for multi-session orchestration with automatic `Fresh` instance lifecycle.
